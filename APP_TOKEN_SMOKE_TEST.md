# App token smoke test

This file was committed by a GitHub App installation token minted by the
`github-token` Cloudflare Worker, to verify it can push commits and open PRs.

Safe to delete — this branch and PR are a test.
