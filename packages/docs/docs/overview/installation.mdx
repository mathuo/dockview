---
id: installation
title: Installation
sidebar_position: 2
description: How to install Dockview for React, Vue, Angular, and vanilla TypeScript.
---

Learn how to install Dockview for a selection of frameworks.

<FrameworkSpecific framework='JavaScript'>
Install the `dockview-core` library:

```sh
npm install dockview-core
```

Then import the stylesheet in your CSS:

```css
@import 'dockview-core/dist/styles/dockview.css';
```

Or in your JavaScript entry point:

```js
import 'dockview-core/dist/styles/dockview.css';
```
</FrameworkSpecific>

<FrameworkSpecific framework='React'>
Install the `dockview-react` library:

```sh
npm install dockview-react
```

Then import the stylesheet in your CSS:

```css
@import 'dockview-react/dist/styles/dockview.css';
```

Or in your JavaScript entry point:

```js
import 'dockview-react/dist/styles/dockview.css';
```
</FrameworkSpecific>

<FrameworkSpecific framework='Vue'>
Install the `dockview-vue` library:

```sh
npm install dockview-vue
```

Then import the stylesheet in your CSS:

```css
@import 'dockview-vue/dist/styles/dockview.css';
```

Or in your JavaScript entry point:

```js
import 'dockview-vue/dist/styles/dockview.css';
```
</FrameworkSpecific>

<FrameworkSpecific framework='Angular'>
Install the `dockview-angular` library:

```sh
npm install dockview-angular
```

Then import the stylesheet in your CSS:

```css
@import 'dockview-angular/dist/styles/dockview.css';
```

Or in your JavaScript entry point:

```js
import 'dockview-angular/dist/styles/dockview.css';
```
</FrameworkSpecific>

Once installed, head to [Core Concepts](/docs/core/overview) to set up your first layout.
